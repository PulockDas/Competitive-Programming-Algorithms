#include <bits/stdc++.h>

using namespace std;

{
    int t, n, i, uu, vv;

    scanf("%d",&t);
    for(i=1; i<=t; i++)
    {
        scanf("%d",&n);
        n--;
        while(n--)
        {
            scanf("%d %d", &uu, &vv);

        }

    }
}
